# Session 2026-06-28 (Audit-Session) — Routing-Regel-vs-PDF-Audit + Migration #21

## Wiedereinstieg-Prompt für nächste Session

> Lies `chat-archive/2026-06-28_session_2.md` der letzten Session.
> Sag mir den Stand und empfiehl, was wir als Nächstes machen.

## Stand

Migration #21 (PDF-Audit-Korrekturen) deployed, getestet, dokumentiert,
gepusht. Branch `main` ist auf `origin/main` (Commit `b605d06`).
HANDOVER um Migrationen #17, #18, #19, #20, #21 synchronisiert (die
4 vorherigen Migrationen waren committed aber noch nicht in der
Migrations-Tabelle eingetragen).

System ist live auf `https://www.myglowmatch.de`. Workflow-ID
`pwSWA5NatKiLhueB`, 27 Nodes. 13 Test-Profile in `test_suite.py`
(11 + Tina + Tom als V5/V6-Coverage), alle mit `-TEST`-Suffix,
Test-Mode-Check-Node blockt Mail-Versand.

## Was in dieser Session passiert ist

### Dreiphasiges Audit (siehe Übergabe `2026-06-28_session.md`)

**Phase 1 — Inventar** (`audit_phase1_inventar.py`): 4 Routing-Tabs
gelesen und als Markdown-Tabelle exportiert (`audit_phase1_inventar.md`).
58 Regeln gesamt: 19 derived_variables, 29 slot_rules (alle aktiv),
3 aktive conflict_rules (+3 inaktiv aus #12), 4 pflegelevel_overrides.
Bug am Rande gefunden: REQ-14.reason-Text war veraltet (sprach noch von
„häufigem Waschen" obwohl Migration #17 die Logik invertiert hat) — im
Sheet auf Migration-#17-Stand synchronisiert.

**Phase 2 — Plausibilitäts-Check** (`audit_phase2_verdacht.md`): 7
Verdachtsfälle 🟡, kein 🔴. Klassen aus dem Trockenshampoo-Bug-Pattern
(Inversionen / Lücken / Über-Trigger / fehlende Negativ-Bedingungen)
durchgegangen ohne PDF-Lesen.

**Phase 3 — PDF-Lesung** (`audit_phase3_befunde.md`): 8 PDFs durch
Agent ausgewertet + Frontend-Check für V4. Ergebnis: 3 echte Bugs
(V1/V5/V6) + 1 PDF-widrige Asymmetrie (V2), V3/V4/V7 als 🟢
verifiziert.

### Migration #21 (PDF-Audit-Korrekturen)

4 Sheet-Edits gebündelt via `migrate_21_pdf_audit.py`:

| Fix | Tab | Edit |
|---|---|---|
| V1 🔴 | `map_derived_variables` | `needs_repair_focus.regel_json`: `gefaerbt` zusätzlich zu `blondiert` (PDF-belegt in 3/4 Bond-IQ-PDFs) |
| V2 🟡 | `map_derived_variables` | `detangling_need.regel_json`: `kraus` zusätzlich zu `lockig` im `leicht_verfilzt`-Zweig (K-08 via entwirrungsspray-Header) |
| V5 🔴 | `map_slot_rules` | REQ-11c NEU (curl_creme bei `heat_use=maybe`) + REQ-04.requires_not erweitert |
| V6 🔴 | `map_slot_rules` | REQ-19b NEU (Volumen-Styling bei `hair_thickness=mittel`) |

Coverage-Profile **Tina** (V5) + **Tom** (V6) zu `test_suite.py`
ergänzt (11 → 13 Profile). Maria deckt V4 (`hair_condition=duenn`)
bereits ab — kein 3. Profil nötig.

### Validation

Pre-Edit-Baseline + Post-Edit-Lauf via `test_suite.py --profile all`,
Set-Vergleich der `final_routine.produkt_key` per neuem
`diff_test_results.py`. Ergebnis: **7/13 Drift, 0 Anomalien**, alle in
Richtung der Bug-Fixes:

- V1 wirkt bei `bianca, lena, maria, silvia, tina, vivien` (alle
  `gefaerbt`, Bond-IQ-Routing wo vorher Renew/Revitalize standen)
- V2 ohne direkte Wirkung — Lena/Sina trafen Entwirrungsspray bereits
  über `trocken`/`deutlich_trocken`. Effekt latent für künftige
  Profile mit `kraus`-only.
- V5 wirkt bei tina (einzige `heat_use=maybe + Curl-Struktur`)
- V6 wirkt bei tom (einzige `mittel + volumen + nicht-Locken`)

Drift-Report: `audit_migration21_drift.md`.

### HANDOVER nachgepflegt

#17, #18, #19, #20 waren in den Commits, aber nicht in HANDOVER-
Migrationstabelle. Jetzt alle 5 (inkl. #21) als Zeilen ergänzt.

## Folgepunkte

Reine Pflege-Themen, kein hartes „muss":

1. **V2-Wirkung in Test-Coverage bestätigen**: kein aktuelles
   Test-Profil isoliert `kraus + leicht_verfilzt`-Bedingung (alle
   kraus-Profile haben entweder `deutlich_trocken` oder `trocken` in
   hair_condition → leicht_verfilzt feuerte schon vor V2). Ein
   Profil wie „kraus + frizz, ohne trocken, ohne deutlich_trocken,
   ohne haarbruch/spliss" wäre ein V2-isolierter Test.
2. **Workflow-Snapshot synchronisieren**: `workflow_live_now.json`
   ist seit Session-Start modified. Audit-irrelevant, aber ein
   frischer GET würde die Doku-Drift schließen.
3. **HANDOVER-Test-Profile-Tabelle erweitern**: HANDOVER hat aktuell
   nicht alle 13 Profile mit Eingabe-Kurzform — Tina + Tom ergänzen,
   damit der Coverage-Stand sichtbar ist.
4. **Phase-3-Vorgehen als Konvention dokumentieren?** Audit-
   Methodik (Inventar → Plausibilität → PDF) lief sehr sauber und
   könnte als wiederverwendbarer Pfad in HANDOVER als T-04 oder
   K-09 stehen, falls späterhin neue Sheet-Tabs/Regel-Klassen
   eingeführt werden.

Nichts davon ist blockierend.

## Artefakte dieser Session

| Datei | Zweck |
|---|---|
| `audit_phase1_inventar.py` | Bulk-Reader für die 4 Routing-Tabs, generiert das Inventar-Markdown |
| `audit_phase1_inventar.md` | Inventar der 58 Regeln (Trigger → Effekt) |
| `audit_phase2_verdacht.md` | 7 Verdachtsfälle 🟡, ohne PDF-Lesen |
| `audit_phase3_befunde.md` | PDF-Belege + Migrations-Empfehlungen |
| `audit_migration21_drift.md` | Pre-vs-Post-Set-Vergleich der 13 Profile |
| `migrate_21_pdf_audit.py` | 4-Edit-Migration mit Backup + Pre/Post-JSON-Check |
| `diff_test_results.py` | Wiederverwendbarer Set-Vergleich zweier `test_results_*.json` |

Backups: `backups/sheets_20260628_224212_pre_migration21/` (CSV der
beiden geänderten Tabs vor Edit).

## Konventionen — neu/bestätigt

Keine neuen K-/T-Konventionen formal. Implizit bestätigt:
- **PDF-strikt** (K-04) hat erneut direkten Output-Effekt — drei
  echte Routing-Bugs wären ohne PDF-Audit unentdeckt geblieben
  (Slot-Drift-Tests sehen sie nicht, weil alle Profile mit
  demselben Bug laufen).
- **Test-Coverage vor Edit prüfen** zahlt sich aus: V4 wäre fast
  als „Toter Code" abgekürzt worden — der Frontend-Check zeigte,
  dass `duenn` lebt und Maria's Profil ihn schon abdeckt.
- **Audit-Delegation an Agent** funktionierte wie erwartet (~10
  PDFs als Schwelle aus Memory) — eine Phase-3-Welle parallel
  durchgegangen statt einzeln nachgelesen.

## Commit/Push-Stand

| Commit | Inhalt |
|---|---|
| `b605d06` | feat: Migration #21 — PDF-Audit-Korrekturen V1/V2/V5/V6 |
| `3d392fb` | docs: Übergabe für Routing-Regel-vs-PDF-Audit (Phase-1-Plan) |

Beide gepusht (`origin/main` = `b605d06`).

## Bewusst NICHT in diese Doku gepackt

- Detailoutput der Bulk-Läufe (in `test_results_*.json`, gitignored)
- Vollständige Regel-Listings (in `audit_phase1_inventar.md`)
- Verdachts-Hypothesen ohne PDF-Klärung (in
  `audit_phase2_verdacht.md`)
- Genauer PDF-Wortlaut (in `audit_phase3_befunde.md`)
- Profile-Schema-Diffs aus Sheet-Edits (in `audit_migration21_drift.md`)
